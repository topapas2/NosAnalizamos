---
title: "NosAnalizamos.Rmd"
author: "Tomás"
date: "2026-05-22"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## Carga de Librerias

```{r}
if (!require("pacman")) install.packages("pacman")

# Instalar todas las librerías necesarias
pacman::p_load(
  knitr,
  tidyverse,
  ggplot2,
  datasets,
  RColorBrewer,
  nycflights13,
  dplyr,
  dbplyr,
  tidyr,
  readr,
  car,
  GGally)
```

## Carga de los Datos

```{r}
# Empezamos por la linea 27 porque realmente los datos comienzan ahí
datos <- read_tsv("data/NosAutoanalizamos2026.tsv",
  skip = 27)

# Sabemos que R si no encuentra nombre para las columnas pone algo asi como "...1", por lo que eliminamos dichas columnas
datos <- datos %>%
  select(!starts_with("..."))
```

## Eliminar Usuario martsobm

```{r}
datos <- datos %>%
  filter(Id != "martsobm")
```

## Modificar Columnas de Notas

```{r}
datos <- datos %>%
  mutate(ANM = case_when(
    ANM == "NC" ~ 3,
    TRUE ~ as.numeric(ANM)
  ))
```







